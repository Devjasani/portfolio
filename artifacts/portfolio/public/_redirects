# ═══════════════════════════════════════════════════════════════════
#  Cloudflare Pages — SPA Redirect Rules
#  File: public/_redirects
#  Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
#  This ensures that deep links like /contact, /about etc. all
#  serve index.html so the React Router handles routing client-side.
#  Without this, refreshing on /contact gives a 404 on Cloudflare.
# ═══════════════════════════════════════════════════════════════════

# SPA fallback — serve index.html for all routes (200, not 301)
/*    /index.html   200
